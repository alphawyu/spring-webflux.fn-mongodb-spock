jwt:
  sessionTime: 86400

logging:
  level:
    org:
      springframework:
        data.mongodb.core: DEBUG
        web.reactive:
          HandlerMapping: DEBUG
          function.client.ExchangeFunctions: DEBUG
      mongodb.driver.protocol.command: TRACE
#      boot.autoconfigure.logging.ConditionEvaluationReportLoggingListener: DEBUG
    com.realworld.webfluxfn: DEBUG
#    root: DEBUG

spring.data:
    mongodb.auto-index-creation: true
